{
  "name": "the-hushabaloo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A read-along picture book for three small listeners.",
  "scripts": {
    "serve": "node tools/serve-site.mjs",
    "test": "playwright test",
    "test:headed": "playwright test --headed --project=desktop",
    "audit": "python tools/audit.py --audio",
    "generate": "python tools/generate.py",
    "build": "python tools/build_standalone.py",
    "check": "python tools/build_player.py && python tools/audit.py --audio && playwright test",
    "master": "python tools/master_voices.py",
    "sfx": "python tools/fetch_sfx.py",
    "build:all": "python tools/fetch_sfx.py && python tools/generate.py && python tools/master_voices.py && python tools/audit.py --audio",
    "player": "python tools/build_player.py"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
